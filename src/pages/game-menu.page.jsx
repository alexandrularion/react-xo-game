import styled from "styled-components";

const GameMenuPage = () => {
  return (
    <Container>
      <p>Game menu page</p>
    </Container>
  );
};

const Container = styled.main`
  display: flex;
`;

export default GameMenuPage;
