import styled from "styled-components";

const RegisterPage = () => {
  return (
    <Container>
      <p>Register Page</p>
    </Container>
  );
};

const Container = styled.main`
  display: flex;
`;

export default RegisterPage;
